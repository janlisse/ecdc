machine:
  services:
    - docker
  java:
    version:
      oraclejdk8
dependencies:
  cache_directories:
    - ~/.sbt
    - ~/.ivy2
    - ~/.m2
test:
  post:
    - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
    - sbt api/docker:publish -Dversion=$CIRCLE_SHA1